export interface User {
  id: string;
  email: string;
  name: string;
  avatar?: string;
  nickname?: string;
  rank?: string;
  lastName?: string;
  firstName?: string;
  department?: string;
  userId?: string;
  provider?: string;
  joinedAt?: string;
}
